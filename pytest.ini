[pytest]
asyncio_mode = auto
testpaths = engine/tests
python_files = test_*.py
pythonpath = .
norecursedirs = legacy archive .* .git .venv node_modules
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
